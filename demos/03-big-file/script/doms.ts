export const uploadInput = document.querySelector(
  "#upload-input"
) as HTMLInputElement;
export const uploadBtn = document.querySelector(
  "#upload-btn"
) as HTMLButtonElement;
export const selectBtn = document.querySelector(
  "#select-btn"
) as HTMLButtonElement;
export const fileNameDisplay = document.querySelector(
  ".file-name-display"
) as HTMLSpanElement;
